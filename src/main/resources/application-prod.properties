# 1. Oracle DB 연결 설정
spring.datasource.url=jdbc:oracle:thin:@yosepdb_high?TNS_ADMIN=/home/ffsd2304/oracleWallet/Wallet_yosepdb
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# 2. DB 계정 정보 (본인이 설정한 계정으로 수정)
spring.datasource.username=ADMIN
spring.datasource.password=gPwlstfg44@qkrdytpq

# 4. (선택사항) SQL 로그 보기 - 디버깅에 큰 도움이 됩니다.
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# 3. JSP 뷰 경로 설정 (앞서 했던 설정)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# 4. 개발 편의 설정
# 콘솔창에 실행되는 SQL 쿼리를 예쁘게 출력해줍니다.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 1. DTO 별칭 설정 (클래스명만 쓰기 위함)
mybatis.type-aliases-package=com.yosep.myweb.**.service

# 2. XML 파일 위치 설정 (이게 없으면 XML을 아예 못 찾습니다)
# src/main/resources/mapper 폴더 안에 xml이 있다면 아래와 같이 적으세요.
mybatis.mapper-locations=classpath:mapper/*.xml

logging.level.org.springframework.web=debug

# SQL 문만 보고 싶을 때
logging.level.com.yosep.myweb.mapper=DEBUG
# SQL 결과(ResultSet)까지 보고 싶을 때
logging.level.org.mybatis=DEBUG

# 자동 재기동 비활성화
spring.devtools.restart.enabled=false

# 세션 유지 시간 설정 (기본 단위는 초 또는 분 설정 가능)
# 예: 30분 동안 활동이 없으면 세션 만료
server.servlet.session.timeout=30m